"use client";

// AdminProfile is now a thin wrapper around the shared ProfileForm so the
// admin and volunteer profile pages share one implementation.
import ProfileForm from "../ProfileForm";

export default ProfileForm;
